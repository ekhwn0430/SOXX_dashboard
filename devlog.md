# 개발 일지 — SOXX 반도체 대시보드

Claude랑 같이 만든 기록. 날짜는 파일 타임스탬프랑 대화 흐름 기준으로 재구성한 거라 초반 며칠은 약간 오차 있을 수 있음.

---

## 2026-07-29 — 시작

`config.py`에 SOXX 상위 20종목(티커/이름/비중) 채워넣는 것부터 시작. 5개는 이미 해뒀었고 나머지 15개를 `SOXX_holdings.csv` 보면서 채움.

그다음 `auth.py`(RSI 봇 프로젝트에서 재활용)가 `config`에서 `TOSS_CLIENT_ID`, `TOSS_CLIENT_SECRET` 같은 값들을 가져오는데 정작 `config.py`엔 없다는 걸 발견. `.env` + `python-dotenv` 조합으로 가기로 하고, RSI 봇 프로젝트의 `.env`를 그대로 복붙. `.gitignore` 만들어서 `.env` 커밋 안 되게 막음.

`TOSS_BASE_URL`을 처음엔 대충 추측(`openapi.tossinvest.com`)해서 넣었다가, "확실한 값으로 알려달라"길래 `www.tossinvest.com`으로 바꿈. (근데 이거 나중에 다시 뒤집힘 — 스포일러: 처음 추측이 맞았음.)

---

## 2026-07-30 — 첫 API 호출, 그리고 404

RSI 봇 프로젝트에서 `data_loader.py`를 가져옴. `config.py`도 RSI 봇 것을 통째로 복붙했다가, "이 대시보드엔 필요 없는 게 너무 많다"는 걸 깨닫고 — RSI/Signal/Entry/PaperTrading/Backtest/텔레그램 관련 상수들 다 쳐내고 캔들 관련 설정만 남김.

`main.py`에 `get_candles("NVDA", "1m", 30)` 한 줄 써서 첫 실행 — **404 에러**. `TOSS_BASE_URL`이 `www.tossinvest.com/oauth2/token`으로 되어 있었는데, 그 경로 자체가 없었음. IP 화이트리스트도 추가해봤지만 소용없었음(애초에 그게 문제가 아니었으니까).

결국 `toss_openapi.json`(Toss Open API 스펙 문서)을 찾아서 확인 — `servers`에 명시된 진짜 주소는 `https://openapi.tossinvest.com`이었음. 처음 추측이 맞았던 거. 고치고 나니 NVDA 1분봉 30개가 드디어 떴다.

---

## 2026-07-31 — 스파크라인, 등락률, 그리고 20종목

"1분봉에서 날짜 바뀌는 거 감지해서 일봉 마감 처리하면 어떨까?" 하는 아이디어를 냈다가, "근데 이 앱 24시간 켜두는 게 아니잖아?" 하고 스스로 문제를 발견. 켰다 껐다 하는 앱이라 날짜 경계를 실시간으로 "감지"할 방법이 없다는 걸 깨달음.

다행히 스펙 문서를 다시 보니 Toss API가 `interval: "1d"`를 **네이티브로 지원**한다는 걸 발견 — 날짜 경계 로직 자체가 필요 없어짐. `get_candles()`에 `1d` 분기를 추가하려고 `_request_candle_page`, `_fetch_candles`를 `interval` 파라미터 받게 리팩터링.

전일 종가 계산: 일봉 마지막 봉은 항상 "진행중"이라고 가정하고 마지막에서 두 번째 봉을 쓰기로 결정(방식 A). `get_quote(symbol)` 함수 만들어서 현재가/전일종가/등락률/스파크라인을 dict 하나로 묶음.

`get_all_quotes()`로 20종목 전체 돌리는 것도 이날 함. 만드는 과정에서 버그 하나 잡음 — 함수에 불필요한 `ticker` 파라미터가 있었고 `stock[ticker]`(변수)를 `stock["ticker"]`(고정 키) 대신 써서 즉시 `TypeError`. 고치고 재테스트하니 19/20 성공, `ON`만 실패. 알고 보니 `config.py` 마지막 항목이 `{"ticker": ""}`로 깨져 있었던 것 — 이후 최신 `SOXX_holdings.csv`로 20종목을 다시 채우면서 자연스럽게 해결(`ON`은 21위로 밀려나고 `CRDO`가 새로 들어옴).

`spec.md`(원래 README였다가 이름 바꿈)에 진행 상황 정리.

---

## 2026-08-01 ~ 2026-08-03 — FastAPI 서버

이 구간 어딘가에서 "우리 이거 언제 시작했지?" 하는 질문이 나와서 파일 타임스탬프 뒤져봄 — 폴더 자체가 7/29에 생겼고, 그날부터 딱 6일째였음.

`dashboard_server.py`(빈 `pass` 파일)에 FastAPI 앱 만들기 시작. FastAPI/uvicorn이 뭔지부터 설명하고(요리사 웨이터 비유), `pip install fastapi uvicorn`. 제일 작은 단위로 `/` 하나만 있는 최소 앱부터 띄워서 확인.

그다음 `/api/quotes` 라우트 — `get_all_quotes()`를 그대로 반환하면 되는지 물어봐서, FastAPI가 dict/list를 알아서 JSON으로 직렬화해준다는 걸 확인(pandas의 `numpy.float64`도 문제없이 직렬화됨). `requirements.txt`도 이때 버전 고정(`requests==2.32.5`, `pandas==3.0.3`, `python-dotenv==1.2.2`, 나중에 `fastapi`/`uvicorn` 추가).

`static/index.html` 정적 서빙 붙이다가 재밌는 해프닝 — Python 코드(`app.mount(...)`)를 실수로 `index.html`에 붙여넣음. 파일 역할(`.py`는 서버 코드, `.html`은 브라우저가 읽는 마크업) 정리해서 다시 나눔. `mount("/")`를 라우트 선언보다 먼저 두면 `/api/quotes`까지 다 가로채서 404 나는 것도 겪고 고침 — Starlette가 등록 순서대로 매칭한다는 걸 배움.

---

## 2026-08-04 — 프론트엔드, 그리고 rate limit과의 싸움

토스증권 앱 스크린샷이랑 `raoni.xyz` 스크린샷을 참고해서 디자인 방향 잡음 — 다크 틸/그린 배경 + 민트 포인트, 상승 빨강/하락 파랑(한국식). raoni는 종목 2개짜리 큰 카드 레이아웃인데 우리는 20개라 그대로 못 가져오는 걸 짚었고, 그림판으로 직접 와이어프레임 그려서 "한 줄짜리 리스트(행) 형태"로 최종 결정. SOXX/SOXL 자체 시세 요약은 스코프 밖이라 "20종목 리스트부터 끝내고 나중에"로 보류.

HTML 구조는 `<table>`로. NVDA 한 줄 하드코딩해서 다크 테마 CSS부터 잡고(HTML/CSS는 사용자가 나중에 따로 배우기로 하고 이 부분은 대신 작성), 실제 30개 스파크라인 좌표를 파이썬으로 미리 계산해서 SVG `polyline`에 박아넣어 디자인 검증. 확인되고 나서 나머지 19종목도 하드코딩하는 대신, `/api/quotes`를 폴링해서 20개 행을 자동 생성하는 JS로 전환 — `fetch()` + `setInterval(10초)` + 스파크라인 좌표 계산 로직을 JS로 이식.

그리고 터진 문제: **"too many request"**. 20종목 × 2호출(1분봉+1일봉) = 40콜을 10초마다 거의 동시에 쏘고 있었던 게 원인. 토큰 자체는 문제 없었음(직접 확인해보니 `expires_in`이 24시간 가까이 됨). 스펙 문서 뒤져서 `MARKET_DATA_CHART` 그룹이 초당 10건 정도로 제한된다는 것과, `/api/v1/prices?symbols=...`라는 배치 조회 엔드포인트(최대 200종목 한 번에)가 있다는 것도 발견했지만, 이건 캔들 시계열은 안 줘서 스파크라인 용도로는 못 씀.

해결책: 전일 종가는 하루에 한 번만 조회하도록 캐싱(40콜 → 20콜), 종목 사이에 0.3초씩 딜레이. 10초 간격으로 4번 연속 돌려서 20/20 성공하는 것까지 확인. 여기에 더해서, 한 사이클이 12~13초 걸려 폴링 주기(10초)보다 길어질 수 있다는 것도 발견해서, 프론트 JS에 "이전 요청 처리중이면 이번 폴링 건너뛰기" 가드까지 추가.

화면 다듬기 이어감 — 표 가운데 정렬(`margin: 0 auto`), 폭 900→1100px(raoni 스크린샷 비율 참고해서 직접 조정). 그다음 보류해뒀던 **SOXX/SOXL 상단 요약 카드** 작업 — raoni 카드에서 색감/타이포 위계는 가져오되, 우리한텐 없는 선물 관련 스탯(펀딩비, 미결제약정 등) 대신 당일 고가/저가로 대체. `config.SUMMARY_TICKERS`, `data_loader.get_summary()`, `/api/summary` 엔드포인트 추가하고 SOXX/SOXL 둘 다 정상 조회되는 것도 확인.

폴링 주기도 손봄 — 요약 카드(가벼움)는 5초, 20종목 표(무거움, 12~13초 소요)는 10초로 따로 가고 싶어서 `REFRESH_SUMMARY_MS`/`REFRESH_QUOTES_MS`로 분리. 처음엔 이 값들을 `config.py`에 넣고 `/api/config`로 JS에 넘기려다가, "이건 브라우저 폴링 주기일 뿐 서버 로직이 쓰는 값이 아니다"로 정리해서 그냥 JS 상수로 둠 — `config.py`의 오래된 `REFRESH_INTERVAL_SEC`(원래도 아무도 안 읽던 값)도 이참에 삭제.

마지막으로 **숫자 롤링 애니메이션** — raoni처럼 가격 갱신될 때 옛값→새값으로 부드럽게 굴러가는 효과. `requestAnimationFrame` + ease-out 보간(0.6초), 값 바뀔 때 상승 초록/하락 빨강으로 잠깐 반짝이는 플래시도 같이. 이거 넣으려면 매 폴링마다 DOM을 통째로 지우고 새로 그리던 방식(`innerHTML = ""`)을 버려야 했음 — 그러면 애니메이션 걸 엘리먼트가 계속 사라지니까. 티커별로 DOM을 한 번만 만들고 캐싱해뒀다가 값만 갱신하는 구조로 리팩터링. 그리고 이 김에 색상도 한국식(상승 빨강/하락 파랑)에서 플래시랑 통일되게 미국식(상승 초록/하락 빨강)으로 전환 — `--up`/`--down` CSS 변수 하나만 바꾸면 텍스트/스파크라인/플래시 색이 전부 같이 바뀌도록 처음부터 짜둔 덕에 한 줄 수정으로 끝남.

---

## 2026-08-05 — 요약 카드 스파크라인, 그리고 탭 시스템

요약 카드(SOXX/SOXL)에도 표처럼 스파크라인 붙임 — 가격/등락률 오른쪽 빈 공간에 배치.

그다음 raoni.xyz의 "지수" 탭이 눈에 들어와서 "우리도 카테고리 만들자, 지수 탭부터"라는 제안이 나옴. 이건 원래 v0 계획엔 없던 확장이라 먼저 스코프 체크하고 진행하기로 함 — v0가 이미 다 끝난 상태라 "의식적인 v1 확장"으로 합의.

raoni는 탭마다 URL을 따로 뒀는데(`/idx` 등), 우리는 로컬 개인용 도구라 그럴 필요 없다고 판단해서 한 페이지 안에서 JS로 탭 전환하는 방식으로 감(페이지 분리는 이 프로젝트 규모엔 오버엔지니어링). 탭 버튼(알약 모양) + `.view`/`.view.active` 토글 구조로 "대시보드"/"지수" 두 탭 우선 만듦. 활성 탭 강조색도 처음엔 민트였다가 차트 상승색(`var(--up)`)으로 통일.

"지수" 탭엔 S&P500/나스닥을 넣기로 했는데, 실제 지수 심볼은 Toss가 캔들 조회를 지원 안 할 가능성이 있어서(raoni도 코스피를 EWY ETF로 대체했던 것처럼) 추종 ETF(SPY, QQQ)로 대체 — 둘 다 실제 조회 확인. 요약 카드 코드랑 거의 똑같아서 중복 안 나게 리팩터링(백엔드는 `_get_quotes_for()` 공통 함수, 프론트는 `createSummaryCard`/`loadCardGrid`를 파라미터로 일반화).

작은 버그 하나 — 대시보드 탭은 20종목 표 때문에 스크롤바가 생기고 지수 탭은 안 생겨서, 두 탭의 콘텐츠 가운데 정렬 위치가 미묘하게 달랐음. `scrollbar-gutter: stable` 한 줄로 해결.

그다음 "SPY 가격을 역산하면 진짜 S&P500 지수가 나오지 않을까" 하는 아이디어가 나옴. 실제로 SPY는 대략 S&P500/10으로 설계돼있긴 한데, raoni 스크린샷을 다시 보니 자기들도 역산값(6,584.56)이랑 네이버 실측값(6,358.95)을 따로 보여주고 있었음 — 3.5%나 차이나는 근사치였던 것. 역산으론 결국 진짜 기준값이 없어서 오차를 못 없애니, 이 방향은 보류하기로.

대신 raoni처럼 네이버에서 환율을 가져올까 하다가, `finance.naver.com`이 이 환경에서 fetch가 아예 안 되고, 사용자가 개발자도구로 뒤져봐도 API를 못 찾음. 근데 Toss 스펙을 다시 뒤져보니 `/api/v1/exchange-rate`가 있었음 — 환율은 그냥 Toss로 해결. 이 함수는 사용자가 직접 짜봤는데, 파라미터 이름 오타(`targetCurrency`→`quoteCurrency`)랑 `raise_for_status()` 누락을 리뷰하면서 잡음. 헤더 우측에 "USD/KRW ₩1,425" 표시 완성. 요약/지수 카드에도 "≈ ₩247,297" 원화 환산 줄 추가, 가격 갱신될 때마다 같이 롤링.

---

## 2026-08-06 — 캐싱으로 구조 전환, 시총비교는 다음으로

`SOXX_holdings.csv`가 또 갱신돼서 20종목 비중/순서 최신화. 종목 자체엔 문제없었는데 검증 스크립트 돌리다가 401/429가 다시 튀어나와서 당황 — 알고 보니 원인이 종목 리스트가 아니라, **테스트 스크립트가 실행되는 동안 사용자의 라이브 대시보드도 동시에 폴링 중이어서 같은 rate limit 예산을 나눠 쓴 것**이었음. 데이터 자체는 정상.

다음 카테고리로 "시총비교"랑 "뉴스"를 만들자는 얘기가 나옴. raoni 스크린샷 보니 시총비교는 1:1 비교(삼성 vs 하이닉스) + 몇 년치 시가총액 추이 차트 + 비율 차트까지 있는, 생각보다 훨씬 큰 기능이었음(다년치 히스토리 데이터 파이프라인 + 제대로 된 차트 라이브러리 필요). "상단 숫자 비교만 v1으로 하고 추이 차트는 빼자"고 스코프를 줄이자고 제안했는데, 이 논의 중에 사용자가 더 급한 문제를 발견 — `REFRESH_QUOTES_MS`를 5000으로 낮췄는데도 표가 SOXX/SOXL 카드처럼 부드럽게 안 움직이는 느낌이라고.

파봤더니: 20종목 조회 자체가 실측 12~13초 걸리는 게 진짜 병목이었음. 폴링 간격을 5초로 낮춰봐야 이전 요청이 아직 안 끝나서 대부분 건너뛰어지고, 실제 갱신 주기는 여전히 12~13초 그대로 — 숫자를 낮추는 것만으론 해결이 안 되는 문제였음. 그래서 시총비교는 잠깐 미뤄두고 이 문제부터 잡기로 함.

해결책: 매 요청마다 라이브로 Toss를 부르는 지금 구조를 버리고, **서버가 백그라운드 스레드에서 미리 주기적으로 갱신해서 캐시에 저장**하고 라우트는 캐시를 즉시 반환하는 구조로 전환(파이썬 부분은 사용자가 직접 작성, 리뷰만 도와줌). 과정에서 버그 두 개 잡음:
1. 처음 버전은 `while True` 루프가 함수로 안 감싸져서 파일 최상단에 그냥 있었음 — 이러면 `data_loader.py`를 `import`하는 순간 무한루프가 바로 돌아서 **import 자체가 영원히 안 끝남**(서버가 아예 못 뜸). 시뮬레이션으로 재현해서 확인시켜줌.
2. summary/index/환율 갱신 뒤 `sleep(5)`, 표 갱신 뒤 `sleep(15)`를 한 루프 안에서 순서대로 실행해서, 실제 한 바퀴가 20초 걸려 "5초 주기"가 이름만 그렇고 실제론 아니었음 — 독립된 루프(스레드) 두 개로 쪼개서 해결.

다 고치고 테스트하다가 또 401/429 폭풍이 재발 — 이번엔 진짜 원인 발견: 백그라운드 스레드가 **프로세스마다** 독립적으로 도는 구조라서, 사용자의 `--reload` 서버 + Claude의 테스트 서버가 동시에 떠있으면 스레드 세트가 두 벌 돌면서 Toss를 두 배로 두드리고 있었던 것. 둘 다 끄고 서버 하나만 켜서 재검증 — `/api/quotes` 응답이 0.07초로 즉시 나오는 것까지 확인. "테스트할 땐 서버가 하나만 떠있는지 확인" 원칙이 이때부터 생김.

캐싱 구조 끝내고 나서 미뤄뒀던 시총비교로 복귀. `/api/v1/stocks?symbols=SOXX,SOXL`에서 발행주식수(SOXL 1억7천만주, SOXX 8천5백만주) 조회 확인. 사용자가 `get_market_cap_comparison()` 함수를 직접 짜다가 버그 몇 개(캔들 엔드포인트를 잘못 복붙, `symbols` 파라미터에 공백, `response` 정의도 안 됐는데 401 체크, `raise_for_status()` 또 빠짐) 나와서 리뷰로 하나씩 잡았고, 마지막 "가격×발행주식수로 시총 계산" 부분은 Claude가 완성. 이미 5초마다 갱신되는 SOXX/SOXL 가격 캐시에다 거의 안 바뀌는 발행주식수만 한 번 캐싱해서 곱하는 거라, 새 백그라운드 루프 없이 깔끔하게 끝남.

프론트는 raoni 스타일로 — 큰 시총 숫자 두 개, 민트/골드 막대그래프(큰 쪽 기준 상대 높이), 하단에 "SOXX가 SOXL보다 $23.2B 더 큼" 격차 문구. 숫자/막대 둘 다 기존이랑 똑같이 부드럽게 굴러가도록 롤링 애니메이션 붙임. 원본 raoni의 몇 년치 추이 차트는 이번에도 스코프 밖으로 남겨둠(다음에 하기로 한 뉴스 탭이 더 큰 작업이라 그 전에 급한 건 아님).

---

## 지금 상태

v0 완성, v1도 탭 시스템 + 지수 탭 + 환율 + 원화 환산 + 서버 캐싱 + 시총비교까지 진행됨. 다음은 뉴스 탭(외부 뉴스 API 필요, 아직 스코프 미정). 자세한 기술적 내용은 `spec.md` 참고.
